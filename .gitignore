node_modules/
dist/
.env
*.tsbuildinfo
coverage/
.vscode/
*.log

# npm pack output / pre-built tarballs (publish via release artifacts, not git)
packages/**/*.tgz
# Crew: ignore runtime state (logs, inbox, sessions)
.crew/orchestration-log/
.crew/log/
.crew/decisions/inbox/
.crew/sessions/
.crew/config.json

# Test temp dirs (created in cwd by vitest tests, cleaned in afterEach)
.test-*
# Docs site generated files
docs/dist/
docs/src/content/docs/reference/api/

# Docs test screenshots (local verification only)
docs/tests/screenshots/
# Crew: SubCrew activation file (local to this machine)
.crew-workstream
.crew/.first-run
.crew/.watch-pids
.crew/.cache/

# Images folder (root only — don't ignore docs/public/images/)
/images/
# Crew: ignore runtime state (logs, inbox, sessions)
.crew/.scratch/

# Crew: PR-1200 picard local artifacts (do not commit)
.pr-body-new.md
.followup-issue-body.md

# .NET build output
bin/
obj/
artifacts/

# Visual Studio IDE artifacts
.vs/
*.user
*.userprefs

# NOTE: .crew/casting/* is AUTHORITATIVE IDENTITY and MUST be committed to main.
# Do not add .crew/casting/ to this ignore list.
