# 2026-03-25T18:11 — EECOM (Core Dev, sonnet)

**Task:** Fix #590 P0 getPersonalCrewRoot() bug  
**Mode:** background  
**Status:** ✅ MERGED

## Outcome

Fixed path mismatch in personal crew resolution. Committed on `crew/590-fix-personal-crew-root`.

## Details

**Root cause:** `getPersonalCrewRoot()` in `consult.ts` used `.crew` as subdirectory, while `resolvePersonalCrewDir()` and `ensurePersonalCrewDir()` in `resolution.ts` use `personal-crew`. Path mismatch broke all personal crew discovery.

**Fix:**
- Updated `consult.ts` to use `personal-crew` subdirectory
- Added 2 new test cases covering both functions
- Confirmed template sync script propagated changes

**Decision:** Personal crew paths must use `personal-crew/` subdirectory (canonical). `.crew` is reserved for project-local crew directories only.

---

*Logged by Scribe at 2026-03-25T18:11Z*
