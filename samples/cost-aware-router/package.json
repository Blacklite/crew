{
  "name": "@crew-samples/cost-aware-router",
  "version": "1.0.0",
  "private": true,
  "description": "Demonstrates Crew SDK cost-aware routing — tier selection, budget tracking, and per-agent cost breakdowns",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@blacklite/crew-sdk": "^0.8.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
