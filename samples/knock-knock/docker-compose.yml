services:
  knock-knock:
    build:
      context: ../..
      dockerfile: samples/knock-knock/Dockerfile
    container_name: crew-knock-knock
    restart: unless-stopped
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
