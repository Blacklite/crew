services:
  rock-paper-scissors:
    build:
      context: ../..
      dockerfile: samples/rock-paper-scissors/Dockerfile
    container_name: crew-rps-arena
    restart: unless-stopped
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
